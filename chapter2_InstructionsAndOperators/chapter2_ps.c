// Q1)

    // Ans) statement i) is invalid. Statement ii) is also invalid cause we can only write 1 character in char datatype variable value.

// "^" this ^ sign is bitwise zor operator in C.

//by default most of the compilers treat decimal value as double datatype value and not float datatype value including the gcc compiler which we are using. // Double gives more precise value then float.

// Q2) 

     // Ans) double datatype. // reason is in line 7 above.

// Q3) 

    // Ans) 

        // #include <stdio.h>
        
        // int main(){
        //     int num;
        //     printf("Enter the number\n");
        //     scanf("%d", &num);
        //     printf("Divisibility test returns: %d\n", num % 97);
        //     return 0;
        // }

// Q4) // related to operator presedence and operator associativity topic.

        // Step by step evaluation of 3 * x / y - z + k
        // where x = 2, y = 3, z = 3 and k = 1

        // result of this expression will be 0.

// Q5)

    // Ans) b) floating point number. (real number) (double datatype)