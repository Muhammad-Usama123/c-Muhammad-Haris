// Decision Making Instructions In C:

    // If-else statement
    // switch statement

// Relational Operators In C:

    // used to evaluate conditions( true or flase)
    // ==, <, >, <=, >=, !=

// Logical Operators:

    // &&, || and !

// "!" this logical operator converts the true into false and false into true.

// 1 is true in c and 0 is false in c. Any non zero value is true in c.

// if ((age<=70 && age>=18) || (vip ==1)) // means "if age is between 70 to 18" or "if vip == 1 " if any of these conditions true then  return true.



// the last "else" in if else, else if, else if is optional and we can use as many else if as we want.

// Operator Precedence:

// 1st priority !
// 2nd priority *,/,%
// 3rd priority +, -
// 4th priority < >, <=, >=
// 5th priority ==, !=
// 6th priority &&
// 7th priority ||
// 8th priority =

// Conditional or ternary Operators:

    // short hand (if-else).

    // condition ? expression-if-true : expression-if-false // In this line "?" and ":" are conditional or ternary operators.

// ASCII integer values when we typecast alphabets into integers. // for details code with chapter 3 ps last question that check whether the entered alphabet is lower case or not?.

